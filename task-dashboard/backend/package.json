{
  "name": "task-dashboard-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.19.2",
    "sqlite3": "^5.1.6",   // Ensure SQLite3 is included
    "sqlite": "^5.1.0",    // sqlite package for async/await
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "start": "node server.js"   // Command to start the backend server
  },
  "engines": {
    "node": ">=14.0.0"  // Specify the Node.js version
  }
}
